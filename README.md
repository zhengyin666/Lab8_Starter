# Lab8-Starter
Name : Zhengyin Yang

Graceful degradation and service workers are related because service workers help a web app keep working when the network is slow or unavailable. When the internet works normally, the app can load everything from the network. But if the connection is bad or offline, the service worker can use cached files instead, so the app still provides a usable experience instead of completely breaking.

My GitHub Published Page is [here](https://zhengyin666.github.io/Lab8_Starter/)
